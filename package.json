{
    "name": "menuapp",
    "version": "0.4.0",
    "description": "Add browser refresh for path /menu. .htaccess working for Apache Web Server. .config working for nginx Web Server. For Github use HashRouter instead of BrowserRouter in index.js",
    "private": false,
    "homepage": "https://rok7803.github.io/menuapp",
    "dependencies": {
        "dotenv": "^16.3.1",
        "gh-pages": "^5.0.0",
        "mobx": "^6.10.0",
        "mobx-react": "^9.0.0",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "react-router-dom": "^6.14.2",
        "react-scripts": "^5.0.1",
        "serve": "^14.2.0"
    },
    "scripts": {
        "start": "react-scripts start",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build",
        "build": "react-scripts build",
        "test": "react-scripts test",
        "eject": "react-scripts eject"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
